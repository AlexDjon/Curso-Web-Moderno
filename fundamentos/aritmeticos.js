const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subt = d - b
const mult = a * b
let divi = d / a
const modu = a % 2

console.log(soma, subt, mult, -divi, modu)

/*
    d++
    ++d
    op1 + op2
*/